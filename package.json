{
  "name": "setu-triage",
  "version": "0.0.0",
  "private": true,
  "description": "Root package for scripts/ — supabase-js resolution for seed/smoke scripts run via web's npm scripts.",
  "scripts": {
    "typecheck:scripts": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
